(in-package :tiny-path.internals)

;;------------------------------------------------------------

;; (defmacro define-tiny-path-kind (name)
;;   `(defclass ,name ()))
;;
;; (defmacro defmacro-tiny-path-dir-kind (name (path-kind) &key seperator)
;;   `(progn
;;      (defclass ,name (dir ,path-kind)
;;        ((seperator :initform ,seperator)))
;;      (defun )))
;; (defmacro defmacro-tiny-path-root-dir-kind (name (path-kind)
;;                                             &key absolute-prefix seperator)
;;   `())

;;------------------------------------------------------------
